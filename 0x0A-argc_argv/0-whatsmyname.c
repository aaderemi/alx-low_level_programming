#include <stdio.h>
/**
 * main - prints the name of the program and a \n
 * @argc: num of arguments
 * @argv: array containin the arguments
 * Return: 0
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[argc - argc]);
	return (0);
}
