#include "main.h"
#include <stdio.h>

int main(void)
{
	char *s = "First, solve the problem. Then, write the code";
	char *f = "";
	char *t;

	t = _strstr(s, f);

	printf("%s\n", t);
	

	return (0);
}
